namespace Platform.Sandbox
{
    public static  class WebSocketTest
    {
        public static void Run()
        {
            //WebSocket ws = new WebSocket();

            //HttpListenerContext c = new HttpListenerContext();

            //c.AcceptWebSocketAsync(

            //HttpListenerWebSocketContext c = new HttpListenerWebSocketContext();

            /*using (HttpListener listener = new HttpListener())
            {
                var context = listener.GetContext();

                var webSocketContext = context.AcceptWebSocketAsync(
            }*/
        }
    }
}
